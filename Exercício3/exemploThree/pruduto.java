import java.lang.Math;

public class produto {

        String nome;
        double valor;
        int quantidade;
    
        produto.calcularValorFinal { private void calcularValorFinal() {{
            double resultadoCalculo;

           resultadoCalculo = (valor * quantidade) + 10%
        }}  

        produto.eletronico{private void eletronico() {(
            double resultadoE;

            resultadoE = (valor * quantidade) + 10%

            }}

        produto.movel{private void movel() { 

            double resultadoM;

            resultadoM = (valor * quantidade)

            if (resultadoM < 10) {resultadoM+11%}
            if (resultadoM >= 5) {resultadoM+5%}

        }